name: Central Workflow

on:
  push:
    branches: [main]

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Call Docker Build Workflow
        uses: actions/workflow-call@v0
        with:
          workflow: video-conversion/.github/workflows/docker-build.yml
          secrets: ${{ toJson(secrets) }}
